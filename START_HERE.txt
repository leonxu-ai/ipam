╔════════════════════════════════════════════════════════════════╗
║                                                                ║
║           中创新航 IPAM 系统 - Claude Code 实施指南           ║
║                                                                ║
╚════════════════════════════════════════════════════════════════╝

📍 当前位置：/opt/ipim/

📁 项目资料：
   ├── calb-ipim/              → 12个完整的HTML设计原型
   ├── PROJECT_BRIEF.md        → 📋 完整需求文档（必读）
   ├── CLAUDE_PROMPT.txt       → 🤖 Claude实施提示词
   └── README.md               → 📖 项目说明

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 快速开始（3步）

1️⃣  阅读需求文档
   cat PROJECT_BRIEF.md

2️⃣  复制下面的提示词

3️⃣  开启新的Claude Code对话，粘贴提示词

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 完整提示词（直接复制到新对话）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

我需要你帮我实现一个企业级IPAM（IP地址管理）系统，用于中创新航工厂的网络IP管理。

## 项目背景

我在 /opt/ipim/calb-ipim/ 目录下有12个完整的前端HTML设计原型（包含登录页、仪表板、IP管理列表、子网管理、审计日志等页面），每个页面都有完整的HTML代码和设计截图。

现在需要基于这些设计原型，实现一个完整的生产级系统。

## 技术要求

### 后端
- Django 5.0.1 + PostgreSQL 15
- django-q2（异步SNMP扫描任务队列）
- pysnmp-lextudio（网络设备SNMP扫描）
- cryptography（SNMP凭证加密）

### 前端
- Django Templates（将HTML原型转换为模板）
- Tailwind CSS 3.4+（离线部署，编译为静态CSS）
- Material Symbols Outlined（离线字体）
- Inter字体（离线部署）
- Alpine.js 3.14+（轻量级交互）
- 暗黑主题为主

### 部署
- Docker + Docker Compose
- **完全离线部署**（无互联网访问）
- 局域网访问地址：http://10.21.111.129
- 所有CDN资源必须本地化

## 核心功能

1. **IP地址管理**：
   - 4种状态：可用、已分配、保留、冲突
   - 并发分配保护（数据库行锁 + CAS原子更新）
   - 自助分配向导（选择子网 → 可视化网格选IP → 填写设备信息）
   - 批量操作

2. **子网管理**：
   - CIDR格式（192.168.10.0/24）
   - 自动初始化IP池
   - 使用率统计

3. **SNMP网络扫描**：
   - 异步扫描ARP表（每10分钟）
   - 支持SNMPv2c/v3
   - 自动冲突检测（MAC地址变化）

4. **审计日志**：
   - 记录所有IP操作
   - 不可修改

5. **仪表板**：
   - 统计卡片、图表
   - 高负载子网监控
   - 最近操作日志

## 关键技术难点

1. **离线Tailwind CSS部署**：
   - 原型HTML使用CDN，需本地编译
   - 提取自定义颜色配置（#2463eb主色、#111621暗黑背景等）

2. **并发IP分配**：
   - 使用 select_for_update() 行锁
   - CAS模式验证原子更新

3. **IP可视化网格**：
   - 256个IP的交互式网格（/24子网）
   - CSS Grid + Alpine.js

4. **浏览器缩放适配**：
   - 支持80%-125%缩放比

## 详细文档

请先阅读 /opt/ipim/PROJECT_BRIEF.md（完整需求文档），然后分析 /opt/ipim/calb-ipim/ 目录下的所有HTML原型。

## 实施要求

1. 严格遵循设计原型（100%还原视觉效果）
2. 生产级代码质量（错误处理、事务保护、并发安全）
3. 完全离线部署（不依赖CDN）
4. 性能要求（10,000 IP<2秒，并发10用户无冲突）

请按以下顺序工作：
1. 分析HTML原型，提取设计规范
2. 设计数据模型
3. 创建Docker配置（绑定到10.21.111.129）
4. 配置离线Tailwind CSS
5. 实现后端核心功能
6. 转换HTML为Django模板
7. 实现SNMP扫描
8. 测试优化

完成后提供离线部署包和文档。现在开始吧！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 提示词复制完成！现在：
   1. 开启新的Claude Code对话
   2. 粘贴上面的提示词
   3. 开始实施

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
